# Set in stage2:
# ---------------------------------------------------------
# set -ef +am
# isFirstRun
# VAR_*
# All functions in /start/functions

modulesDirContent="$(ls -A "$VAR_MODULES_DIR")"
if [ ! -z "$modulesDirContent" ] && [ "$modulesDirContent" != ".snapshot" ]
then
   cp -a $VAR_MODULES_DIR/* "$VAR_WEBAPPS_DIR/ROOT/WEB-INF/lib/"
fi
