# Set in stage2:
# ---------------------------------------------------------
# set -ef +am
# isFirstRun
# VAR_*
# All functions in /start/functions

if [ ! -z "$(ls -A "$VAR_MODULES_DIR")" ]
then
   cp -a $VAR_MODULES_DIR/* "$VAR_WEBAPPS_DIR/ROOT/WEB_INF/lib/"
fi
