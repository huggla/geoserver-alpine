# Set in stage2:
# ---------------------------------------------------------
# set -e +a +m +s +i -f
# isFirstRun
# VAR_*
# All functions in /start/functions

if [ "$isFirstRun" == "true" ]
then
   runBinCmdAsLinuxUser '/bin/sh - << eof
      . /start/functions/replaceFileWithLink
      replaceFileWithLink "$VAR_WEBAPPS_DIR/geoserver/WEB-INF/web.xml" "$VAR_CONFIG_DIR/web.xml"
eof'
fi
