# Set in stage2:
# ---------------------------------------------------------
# set -ef +am
# isFirstRun
# VAR_*
# All functions in /start/functions

modulesDirContent="$(ls -A "$VAR_MODULES_DIR" | sed 's/.snapshot\>//' | xargs)"
if [ ! -z "$modulesDirContent" ]
then
   cp -a $VAR_MODULES_DIR/* "/usr/local/tomcat/webapps/geoserver/WEB-INF/lib/"
fi
