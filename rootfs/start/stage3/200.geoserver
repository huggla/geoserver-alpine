# Set in stage2:
# ---------------------------------------------------------
# set -e +a +m +s +i -f
# isFirstRun
# VAR_*
# All functions in /start/functions

if [ ! -s "/usr/local/tomcat/webapps/geoserver/WEB-INF/web.xml" ]
then
   initGeoserver
fi
